# Reference material from the original artist — NEVER commit any of it.
# (The artwork belongs to @cosmodernism; we only study it locally.)
cool art.mov
research/
frames/
**/frames/
**/frames2/
*.png
*.jpg
*.jpeg
*.gif

# Generated recordings & renders
*.webm
*.mp4
*.mov
output.*

# Node / OS
node_modules/
.DS_Store

# Claude / tooling files — never commit
CLAUDE.md
.claude/
.claude*
.mcp.json
memory/
